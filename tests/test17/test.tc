
/*
from std.string import String
from std.io import println
from std.unit.test import TestUnit, UnitSet
*/

fn f1(x: u32) -> fn(x: u32) -> fn (z: u32) -> u32 {
    let alpha: u32 = 10
    fn f2(y: u32) -> fn (z: u32) -> u32 {
        fn f3(z: u32) -> u32 {
            return x
        }
        return f3
    }

    return f2
}