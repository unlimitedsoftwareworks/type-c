

fn loop(x: u32[]) -> (u32, bool) {
    let i: u64 = 0

    while i < x.length {
        let j = i+1
        x = yield (x[i], j == x.length)
        

        if j == x.length {
            x = yield (x[i], true)
        }
    }
}

fn main() -> u32 {

    return 0
}