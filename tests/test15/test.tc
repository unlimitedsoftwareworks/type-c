from std.io import println
from std.string import String

fn fib(x: u32) -> u32 {
    if x < 2  {
        return x
    }
    return fib(x - 1) + fib(x - 2)
}

fn main() {
    let x: u32 = 40
    let y: u32 = fib(x)

    println("fib = " + y)
}