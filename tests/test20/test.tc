
/*
from std.string import String
from std.io import println
from std.unit.test import TestUnit, UnitSet
*/

fn closureTest(y: u32) {
    let z: u32 = 10
    let f2 = fn(x: u32) {
        return z + x
    }

    return f2
}

type T = class {
    fn init(x: u32) {
    }
}

let y = closureTest(20)(10)
let x = closureTest(30)(20)
let z = new T()
let a = new T()
