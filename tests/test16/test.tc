

type ConcreteUserData = class {
    let age: i32

    fn init() {
        this.age = 1
    }

    fn cool(x: i32) -> i32[] {
        return [x, x+1]
    }
}

let cud = new ConcreteUserData()

let z: interface { fn cool(x: i32) -> i32[] } = cud