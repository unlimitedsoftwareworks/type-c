

type ConcreteUserData<T> = class {
    let age: T

    fn init(v: T) {
        this.age = v
    }

    fn cool() -> T[] {
        return [this.age, this.age+1]
    }
}

let cud = new ConcreteUserData()
